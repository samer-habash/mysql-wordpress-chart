 Updated a secret that will be randomly generated for MYSQL ROOT PASS .

 The wordpress will crash after a while , mysql connection errors.

 #I had some trouble of pvc that is not provisioned/bounded in my laptop , Github refers it is minikube new versions :
 So I run the minikube as : minikube start --driver=docker --disk-size=50g 

 Deleted the liveprob and readiness in deployments because they are unhealthy and the pods will never be ready state , they will start and created but not ready.

 helpers.tpl file has the dynamic names/labels/selectorLabels/ etc ... which is defined globally accross the Chart mysql-wordpress

 The file helm_template_before_installation.yaml describe the output of "helm template ./mysql-wordpress .
