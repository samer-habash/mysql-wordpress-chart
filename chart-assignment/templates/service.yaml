apiVersion: v1
kind: Service
metadata:
  name: {{ include "chart-assignment.fullname" . }}
  labels:
    {{- include "chart-assignment.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "chart-assignment.selectorLabels" . | nindent 4 }}
