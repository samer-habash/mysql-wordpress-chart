apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "chart-assignment.fullname" . }}-test-connection"
  labels:
    {{- include "chart-assignment.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "chart-assignment.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
